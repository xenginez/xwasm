#include "runtime.h"

xwasm::runtime::runtime()
{

}

xwasm::runtime::~runtime()
{

}

void xwasm::runtime::init()
{
	for( size_t i = 0; i < _module.mem_size(); i++ )
	{

	}
	for( size_t i = 0; i < _module.global_size(); i++ )
	{

	}
	for( size_t i = 0; i < _module.tabel_size(); i++ )
	{

	}
}
